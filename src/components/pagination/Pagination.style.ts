import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  pagination: {
    backgroundColor: '#000',
    color: '#ffe300',
    textAlign: 'center',
    fontWeight: 'bold',
    paddingTop: 10,
    paddingBottom: 10,
  },
})
